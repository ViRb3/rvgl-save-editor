{
  "name": "rvgl-save-editor",
  "version": "1.0.0",
  "private": true,
  "description": "RVGL Save Game Editor — Import, edit, and export save files as ZIP",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "mkdir -p dist",
    "build:js": "esbuild src/app.js --bundle --format=iife --platform=browser --target=es2020 --outfile=dist/app.bundled.js && terser dist/app.bundled.js --compress --mangle --output dist/app.min.js && rm dist/app.bundled.js",
    "build:css": "esbuild src/styles.css --minify --outfile=dist/styles.css",
    "build:html": "html-minifier-terser --collapse-whitespace --remove-comments --collapse-boolean-attributes --minify-css true --minify-js true -o dist/index.html src/index.html",
    "build": "npm run clean && npm run build:js && npm run build:css && npm run build:html",
    "dev": "npm run build:html && npm run build:css && esbuild src/app.js --bundle --format=iife --platform=browser --target=es2020 --outfile=dist/app.min.js --watch",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,css,html,json,md,yml,yaml}": "prettier --write"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "html-minifier-terser": "^7.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "terser": "^5.46.0"
  }
}
